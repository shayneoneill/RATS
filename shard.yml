name: RATS
version: 0.1.0

authors:
  - Shayne <shayne dot oneill at quantifytechnology dot com>

description: |
  Rampantly Aggressive Telemetry Server

dependencies:
  pg:
    github: will/crystal-pg
    version: "~> 0.5"
  dotenv:
    github: gdotdesign/cr-dotenv

# development_dependencies:
#   webmock:
#     github: manastech/webmock.cr

# license: MIT
